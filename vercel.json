{
  "version": 2,
  "installCommand": "npm install -g pnpm@10.13.1 && pnpm install --frozen-lockfile",
  "buildCommand": "cd apps/web && pnpm run vercel-build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "true",
    "NODE_ENV": "production",
    "VERCEL": "1"
  },
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "true"
    }
  }
}
{
  "version": 2,
  "installCommand": "npm install -g pnpm@10.13.1 && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=astral-core-web",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "true",
    "NODE_ENV": "production",
    "VERCEL": "1"
  },
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "true"
    }
  }
}
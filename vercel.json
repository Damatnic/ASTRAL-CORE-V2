{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm -F astral-core-web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "true",
    "NODE_ENV": "production",
    "VERCEL": "1"
  },
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "true"
    }
  }
}
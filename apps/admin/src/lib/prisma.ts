/**
 * ASTRAL_CORE 2.0 - Admin Prisma Client Configuration
 * 
 * This file configures the Prisma client for the admin application,
 * leveraging the centralized database package for consistency.
 */

import { prisma } from '@astralcore/database';

// Re-export the centralized prisma instance
export { prisma };
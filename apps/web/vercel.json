{
  "installCommand": "cd ../.. && npm install -g pnpm@10.13.1 && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter astral-core-web build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "true",
    "NODE_ENV": "production",
    "VERCEL": "1"
  }
}
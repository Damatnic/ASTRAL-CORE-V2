{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "npm run vercel-build",
  "env": {
    "SKIP_ENV_VALIDATION": "true",
    "NODE_ENV": "production"
  },
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "true"
    }
  },
  "functions": {
    "src/app/api/**/*.js": {
      "maxDuration": 30
    }
  }
}